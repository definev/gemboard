import 'package:cell/cell.dart';
import 'package:flutter/material.dart';

class UrlCellView extends StatelessWidget {
  const UrlCellView({
    super.key,
    required this.cell,
  });

  final UrlCell cell;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
