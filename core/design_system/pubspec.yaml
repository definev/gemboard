name: design_system

description: "design system for gemboard"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  copy_with_extension: 
  
  flutter_hooks: ^0.20.5
  equatable: 
  flextras:
  smooth_sheets: 
  sliver_tools: ^0.2.12
  flutter_portal:
  flutter_animate:

  flutter_markdown:
  flutter_markdown_latex:
    git:
      url: https://github.com/definev/flutter_markdown_latex.git
      ref: main
  flutter_highlighter:

  url_launcher: 

  super_sliver_list: ^0.4.1
  boundless_stack:
    git:
      url: https://github.com/definev/boundless_stack.git
      ref: main

  google_fonts: 
  mix: ^1.4.0
  iconly: ^1.0.1
  line_icons: ^2.0.3

  bitsdojo_window: ^0.1.6

  flutter_shaders: ^0.1.2
  emoji_selector:
    git:
      url: https://github.com/definev/flutter-emoji-selector.git
      ref: main
  toastification: ^2.1.0
  super_drag_and_drop: ^0.8.19

  go_router: ^14.2.1

dependency_overrides: 
  flutter_math_fork:
    git:
      url: https://github.com/dessangithub/flutter_math_fork.git
      ref: fix_hashValues-to-Object.hash

dev_dependencies:
  copy_with_extension_gen: 
  build_runner: ^2.4.11 

flutter:
  uses-material-design: true
  shaders:
    - assets/shaders/grid_background.frag