part of 'toolbar.dart';

class DSToolbarKind extends Variant {
  const DSToolbarKind(super.name);

  static const vertical = DSToolbarKind('vertical');
  static const horizontal = DSToolbarKind('horizontal');
}
