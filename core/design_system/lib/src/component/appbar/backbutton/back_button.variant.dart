part of 'back_button.dart';

class DSBackButtonKind extends Variant {
  const DSBackButtonKind(super.name);

  static const close = DSBackButtonKind('close');
  static const back = DSBackButtonKind('back');
}
