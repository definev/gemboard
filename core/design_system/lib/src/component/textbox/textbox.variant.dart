part of 'textbox.dart';

class DSTextboxKind extends Variant {
  const DSTextboxKind(super.name);

  static const DSTextboxKind outline = DSTextboxKind('outline');
  static const DSTextboxKind boundless = DSTextboxKind('boundless');
}