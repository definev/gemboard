import 'package:flutter/material.dart';

class GemboardDatabaseView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('gemboard_database'),
      ),
      body: Center(
        child: Text('Hello, gemboard_database!'),
      ),
    );
  }
}